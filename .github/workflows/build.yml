name: Build Image

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Clone application repo
        run: git clone https://github.com/petrowictor/spring-boot-api app

      - name: Login Docker
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login \
          -u "${{ secrets.DOCKER_USER }}" \
          --password-stdin

      - name: Build image
        run: |
          cd app
          docker build -t petrowictor/api-app:latest .

      - name: Push image
        run: docker push mpetrowictor/api-app:latest